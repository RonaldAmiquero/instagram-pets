import React from 'react';
import { AnimationWrapper, StyledLoader } from './styles';

export const Loader = () => (
  <AnimationWrapper>
    <StyledLoader>
      <div />
      <div />
    </StyledLoader>
  </AnimationWrapper>
);
